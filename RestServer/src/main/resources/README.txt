HOW TO USE

* maven clean build the project
* call mvn spring-boot:run to run application

For swagger ui go to http://localhost:8080/swagger-ui.html
For singular rest responses go to http://localhost:8080/{rest requestmethod}